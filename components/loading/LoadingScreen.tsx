"use client";

/**
 * Legacy entry — production uses the provided Entropi MP4 via VideoLoadingScreen.
 * Kept for import compatibility; do not restore SVG liquid as the primary loader.
 */
export { VideoLoadingScreen as LoadingScreen } from "@/components/loading/VideoLoadingScreen";
